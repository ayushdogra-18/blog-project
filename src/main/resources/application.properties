#spring.application.name=blogProject
#spring.datasource.url=jdbc:postgresql://localhost:5432/blogproject
#spring.datasource.username=postgres
#spring.datasource.password=ayush
#spring.jpa.hibernate.ddl-auto=update
#
##spring.main.banner-mode=off
##logging.level.root=OFF

#
#spring.application.name=blogProject
#spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/blogproject}
#spring.datasource.username=${DATABASE_USERNAME:postgres}
#spring.datasource.password=${DATABASE_PASSWORD:ayush}
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=${PORT:8080}